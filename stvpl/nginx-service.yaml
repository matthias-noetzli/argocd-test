apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: nginx
  name: nginx
spec:
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 80
  selector:
    io.kompose.service: nginx
